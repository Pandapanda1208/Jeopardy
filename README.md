To customize, edit the Jeopardy_data.json file, it is pretty self explanatory, don't touch the color section as changing the color will make it gray, the same gray as when it has already been clicked on.

(Currently doesn't work) If you want a question to be a daily double, change the double section to be 'true', no Caps, the already selected color will be red for this question.

(Currently doesn't work) At the bottom there will be a section that you can change to true if you want a second board-categories 6 through 10- otherwise adding data to those categories will be useless.
